{
  "name": "snip-bundle",
  "version": "1.0.0",
  "description": "Snip — generated release bundle (API + web UI + CLI).",
  "private": true,
  "scripts": {
    "start": "bun server.js"
  },
  "bin": {
    "snip": "cli.js"
  }
}
